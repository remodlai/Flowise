"/attachments/{id}":
  get:
    summary: "INTERNAL: Get Attachment By ID (Non-Operational)"
    description: |
      INTERNAL: **NON-OPERATIONAL.** Defined in router, but the controller handler is missing.
      Intended to retrieve a specific attachment by its ID.
    operationId: "internalAttachmentsGetById"
    tags: ["Attachments", "InternalAPI", "NonOperational"]
    parameters:
      - name: id
        in: path
        required: true
        description: "ID of the attachment to retrieve."
        schema:
          type: string
    responses:
      '500':
        description: "Internal Server Error (Likely due to missing handler)."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"
    # security:
    #   - InternalApiKeyAuth: []
