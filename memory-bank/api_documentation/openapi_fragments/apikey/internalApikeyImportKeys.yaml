"/apikey/import":
  post:
    summary: "INTERNAL: Import API Keys"
    description: "INTERNAL: Imports API keys from a JSON file (data URI). Supports different import modes."
    operationId: "internalApikeyImportKeys"
    tags: ["API Keys", "InternalAPI"]
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../../schemas/modules/ApiKeySchemas.yaml#/components/schemas/ApiKeyImportRequest"
    responses:
      '200':
        description: "API Keys imported successfully. Returns the full list of API keys."
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "../../schemas/modules/ApiKeySchemas.yaml#/components/schemas/ApiKeyWithCount"
      '412':
        description: "Precondition Failed (e.g., missing jsonFile)."
        content:
          application/json:
            schema:
              $ref: '../../schemas/shared/ErrorResponse.yaml#/components/schemas/ErrorResponse'
      '500':
        description: "Internal Server Error (e.g., invalid data URI, JSON parse error, import conflict)."
        content:
          application/json:
            schema:
              $ref: '../../schemas/shared/ErrorResponse.yaml#/components/schemas/ErrorResponse'
    security:
      - ApiKeyAuth: []
