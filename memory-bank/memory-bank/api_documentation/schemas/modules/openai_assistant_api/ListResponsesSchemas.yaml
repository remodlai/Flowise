components:
  schemas:
    OpenAIAssistantListResponse:
      type: object
      properties:
        object:
          type: string
          enum: [list]
        data:
          type: array
          items:
            $ref: "#/components/schemas/OpenAIAssistant" # From AssistantSchemas.yaml
        first_id:
          type: string
          nullable: true
        last_id:
          type: string
          nullable: true
        has_more:
          type: boolean
      required: [object, data, first_id, last_id, has_more]

    # Add other list response wrappers here as needed (e.g., for Messages, Runs, Files etc.)
